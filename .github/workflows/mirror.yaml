name: ♻️ Sync 🗃️

permissions:
  contents: write

on:
  workflow_dispatch:
  schedule:
   - cron: "0 */1 * * *"  # Every 01 Hr

jobs:
  mirror-repo:
    uses: "pkgforge-community/repo-data/.github/workflows/sync_repos_git.yaml@main"
    with:
      upstream-git-repo: "https://git.suyu.dev/suyu/suyu"
